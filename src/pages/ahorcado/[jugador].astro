---
import ErrorPage from "@/components/ErrorPage.astro"

export const prerender = true;
export function getStaticPaths() {
  return [
    {params: {jugador: 'amigos'}},
    {params: {jugador: 'pareja'}},
  ];
}

const { jugador } = Astro.params;
---
<ErrorPage />
